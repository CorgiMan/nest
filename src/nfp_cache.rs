use part::Part;

pub struct NFPCache {
    pub parts: Vec<Part>,
    //  cache: HashMap<...>,
}

impl NFPCache {
    // fn new() {}
    // fn get_nfp(ix_a, rot_a, ix_b, rot_b) {}
    // fn calc_nfp(ix_a, rot_a, ix_b, rot_b) {}
}
